<h1><%= @group.name %></h1>

<h3>Below are the items you have stored in <%= @group.name %>:</h3>

<ul>
  <% @items.each do |item| %>
    <li><a href="/items/<%= item.slug %>"><%= item.name %></li>
  <% end %>
</ul>

<br><br>

<b><a href="/items/new" class="button btn-primary">Add an item</a></b>
